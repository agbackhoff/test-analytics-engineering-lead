analytics_test:
  outputs:
    dev:
      dataset: raw_marketing_data
      job_execution_timeout_seconds: 300
      job_retries: 1
      keyfile: "{{ env_var('GOOGLE_APPLICATION_CREDENTIALS') }}"
      location: US
      method: service-account
      priority: interactive
      project: test-analytics-lead-adrian
      threads: 4
      type: bigquery
  target: dev
